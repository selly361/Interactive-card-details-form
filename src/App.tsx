import { Card, Form, SideBar } from 'components';
import React, { Fragment } from 'react';

function App() {
  return (
    <Fragment>
      <SideBar />
      <Card />
      <Form />
    </Fragment>
  );
}

export default App;
